#cloud-config

#Specify the user
users:
  - default
  - name: ubuntu 
    lock_passwd: False
    sudo: ["ALL=(ALL) NOPASSWD:ALL\nDefaults:root !requiretty"]
    shell: /bin/bash

write_files:
  - content: |
        #!/bin/sh
        DEBIAN_FRONTEND=noninteractive sudo apt-get -qqy update
        DEBIAN_FRONTEND=noninteractive sudo apt-get install -qqy git 
        sudo chown ubuntu:ubuntu /home/ubuntu
        cd /home/ubuntu
        git clone https://github.com/peploleum/deeploy.git
        cd deeploy/freeipa/client/
        ./install-ubuntu-18.04.sh
    path: /home/ubuntu/deploy_freeipa.sh
    permissions: 0777

# run commands
runcmd:
 - su -l ubuntu ./deploy_freeipa.sh
